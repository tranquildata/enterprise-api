components:

  schemas:

    ServiceList:
      type: object
      properties:
        services:
          type: array
          items:
            type: string
      required:
        - services

    PeerDetail:
      type: object
      properties:
        identifier:
          type: integer
        certificate:
          type: string
      required:
        - identifier
        - certificate